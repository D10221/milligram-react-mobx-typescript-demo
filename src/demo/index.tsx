import * as React from "react";
import * as ReactDOM from "react-dom";
import { App } from "./App";

document.title = "Miligram React Elements";
ReactDOM.render(
    <App />, document.getElementById("root")
);
